# Admin token — create/list API users (POST/GET /api/admin/users)
ADMIN_TOKEN=change-me-admin-secret

# Legacy alias (optional): same as ADMIN_TOKEN if ADMIN_TOKEN is empty
API_TOKEN=

FLASK_HOST=127.0.0.1
FLASK_PORT=5000
FLASK_DEBUG=1
